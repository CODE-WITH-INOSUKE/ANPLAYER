<%- include('partials/header') %>
<div class="page home-page">
  <div class="hero">
    <h1 class="hero-title">ANPlayer</h1>
    <p class="hero-subtitle">Premium Music Streaming</p>
  </div>

  <% if (continueListening.length > 0) { %>
  <section class="section">
    <div class="section-header">
      <h2>Continue Listening</h2>
      <a href="/library?section=recent" class="see-all">See All</a>
    </div>
    <div class="card-row" id="continueRow">
      <% continueListening.forEach(function(song) { %>
      <div class="card" onclick="loadSong({id: <%= song.id %>, title: '<%= song.title.replace(/'/g, "\\'") %>', artist: '<%= song.artist.replace(/'/g, "\\'") %>', album: '<%= (song.album || '').replace(/'/g, "\\'") %>', duration: <%= song.duration %>, artwork: '<%= song.artwork || '' %>', youtube_id: '<%= song.youtube_id %>'})">
        <div class="card-artwork">
          <% if (song.artwork) { %>
          <img src="<%= song.artwork %>" alt="" loading="lazy">
          <% } else { %>
          <div class="card-artwork-placeholder">
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="12" cy="12" r="10"/><polygon points="10 8 16 12 10 16 10 8"/></svg>
          </div>
          <% } %>
          <button class="card-play-btn" onclick="event.stopPropagation(); loadSong({id: <%= song.id %>, title: '<%= song.title.replace(/'/g, "\\'") %>', artist: '<%= song.artist.replace(/'/g, "\\'") %>', album: '<%= (song.album || '').replace(/'/g, "\\'") %>', duration: <%= song.duration %>, artwork: '<%= song.artwork || '' %>', youtube_id: '<%= song.youtube_id %>'})">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><polygon points="5 3 19 12 5 21 5 3"/></svg>
          </button>
        </div>
        <div class="card-info">
          <div class="card-title"><%= song.title %></div>
          <div class="card-subtitle"><%= song.artist %></div>
        </div>
      </div>
      <% }) %>
    </div>
  </section>
  <% } %>

  <% if (recentlyPlayed.length > 0) { %>
  <section class="section">
    <div class="section-header">
      <h2>Recently Played</h2>
      <a href="/library?section=recent" class="see-all">See All</a>
    </div>
    <div class="card-row">
      <% recentlyPlayed.forEach(function(song) { %>
      <div class="card" onclick="loadSong({id: <%= song.id %>, title: '<%= song.title.replace(/'/g, "\\'") %>', artist: '<%= song.artist.replace(/'/g, "\\'") %>', album: '<%= (song.album || '').replace(/'/g, "\\'") %>', duration: <%= song.duration %>, artwork: '<%= song.artwork || '' %>', youtube_id: '<%= song.youtube_id %>'})">
        <div class="card-artwork">
          <% if (song.artwork) { %>
          <img src="<%= song.artwork %>" alt="" loading="lazy">
          <% } else { %>
          <div class="card-artwork-placeholder">
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="12" cy="12" r="10"/><polygon points="10 8 16 12 10 16 10 8"/></svg>
          </div>
          <% } %>
          <button class="card-play-btn" onclick="event.stopPropagation(); loadSong({id: <%= song.id %>, title: '<%= song.title.replace(/'/g, "\\'") %>', artist: '<%= song.artist.replace(/'/g, "\\'") %>', album: '<%= (song.album || '').replace(/'/g, "\\'") %>', duration: <%= song.duration %>, artwork: '<%= song.artwork || '' %>', youtube_id: '<%= song.youtube_id %>'})">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><polygon points="5 3 19 12 5 21 5 3"/></svg>
          </button>
        </div>
        <div class="card-info">
          <div class="card-title"><%= song.title %></div>
          <div class="card-subtitle"><%= song.artist %></div>
        </div>
      </div>
      <% }) %>
    </div>
  </section>
  <% } %>

  <% if (favorites.length > 0) { %>
  <section class="section">
    <div class="section-header">
      <h2>Favorites</h2>
      <a href="/library?section=favorites" class="see-all">See All</a>
    </div>
    <div class="card-row">
      <% favorites.forEach(function(song) { %>
      <div class="card" onclick="loadSong({id: <%= song.id %>, title: '<%= song.title.replace(/'/g, "\\'") %>', artist: '<%= song.artist.replace(/'/g, "\\'") %>', album: '<%= (song.album || '').replace(/'/g, "\\'") %>', duration: <%= song.duration %>, artwork: '<%= song.artwork || '' %>', youtube_id: '<%= song.youtube_id %>'})">
        <div class="card-artwork">
          <% if (song.artwork) { %>
          <img src="<%= song.artwork %>" alt="" loading="lazy">
          <% } else { %>
          <div class="card-artwork-placeholder">
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="12" cy="12" r="10"/><polygon points="10 8 16 12 10 16 10 8"/></svg>
          </div>
          <% } %>
          <button class="card-play-btn" onclick="event.stopPropagation(); loadSong({id: <%= song.id %>, title: '<%= song.title.replace(/'/g, "\\'") %>', artist: '<%= song.artist.replace(/'/g, "\\'") %>', album: '<%= (song.album || '').replace(/'/g, "\\'") %>', duration: <%= song.duration %>, artwork: '<%= song.artwork || '' %>', youtube_id: '<%= song.youtube_id %>'})">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><polygon points="5 3 19 12 5 21 5 3"/></svg>
          </button>
        </div>
        <div class="card-info">
          <div class="card-title"><%= song.title %></div>
          <div class="card-subtitle"><%= song.artist %></div>
        </div>
      </div>
      <% }) %>
    </div>
  </section>
  <% } %>

  <% if (downloads.length > 0) { %>
  <section class="section">
    <div class="section-header">
      <h2>Downloads</h2>
      <a href="/library?section=downloads" class="see-all">See All</a>
    </div>
    <div class="card-row">
      <% downloads.forEach(function(song) { %>
      <div class="card" onclick="loadSong({id: <%= song.id %>, title: '<%= song.title.replace(/'/g, "\\'") %>', artist: '<%= song.artist.replace(/'/g, "\\'") %>', album: '<%= (song.album || '').replace(/'/g, "\\'") %>', duration: <%= song.duration %>, artwork: '<%= song.artwork || '' %>', youtube_id: '<%= song.youtube_id %>'})">
        <div class="card-artwork">
          <% if (song.artwork) { %>
          <img src="<%= song.artwork %>" alt="" loading="lazy">
          <% } else { %>
          <div class="card-artwork-placeholder">
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="12" cy="12" r="10"/><polygon points="10 8 16 12 10 16 10 8"/></svg>
          </div>
          <% } %>
          <button class="card-play-btn" onclick="event.stopPropagation(); loadSong({id: <%= song.id %>, title: '<%= song.title.replace(/'/g, "\\'") %>', artist: '<%= song.artist.replace(/'/g, "\\'") %>', album: '<%= (song.album || '').replace(/'/g, "\\'") %>', duration: <%= song.duration %>, artwork: '<%= song.artwork || '' %>', youtube_id: '<%= song.youtube_id %>'})">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><polygon points="5 3 19 12 5 21 5 3"/></svg>
          </button>
        </div>
        <div class="card-info">
          <div class="card-title"><%= song.title %></div>
          <div class="card-subtitle"><%= song.artist %></div>
        </div>
      </div>
      <% }) %>
    </div>
  </section>
  <% } %>

  <% if (trendingSongs.length > 0) { %>
  <section class="section">
    <div class="section-header">
      <h2>Trending</h2>
      <a href="/library?section=most" class="see-all">See All</a>
    </div>
    <div class="card-row">
      <% trendingSongs.forEach(function(song) { %>
      <div class="card" onclick="loadSong({id: <%= song.id %>, title: '<%= song.title.replace(/'/g, "\\'") %>', artist: '<%= song.artist.replace(/'/g, "\\'") %>', album: '<%= (song.album || '').replace(/'/g, "\\'") %>', duration: <%= song.duration %>, artwork: '<%= song.artwork || '' %>', youtube_id: '<%= song.youtube_id %>'})">
        <div class="card-artwork">
          <% if (song.artwork) { %>
          <img src="<%= song.artwork %>" alt="" loading="lazy">
          <% } else { %>
          <div class="card-artwork-placeholder">
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="12" cy="12" r="10"/><polygon points="10 8 16 12 10 16 10 8"/></svg>
          </div>
          <% } %>
          <button class="card-play-btn" onclick="event.stopPropagation(); loadSong({id: <%= song.id %>, title: '<%= song.title.replace(/'/g, "\\'") %>', artist: '<%= song.artist.replace(/'/g, "\\'") %>', album: '<%= (song.album || '').replace(/'/g, "\\'") %>', duration: <%= song.duration %>, artwork: '<%= song.artwork || '' %>', youtube_id: '<%= song.youtube_id %>'})">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><polygon points="5 3 19 12 5 21 5 3"/></svg>
          </button>
        </div>
        <div class="card-info">
          <div class="card-title"><%= song.title %></div>
          <div class="card-subtitle"><%= song.artist %></div>
        </div>
      </div>
      <% }) %>
    </div>
  </section>
  <% } %>

  <% if (albums.length > 0) { %>
  <section class="section">
    <div class="section-header">
      <h2>Albums</h2>
      <a href="/library?section=albums" class="see-all">See All</a>
    </div>
    <div class="card-row">
      <% albums.forEach(function(album) { %>
      <div class="card" onclick="window.location.href='/library/albums/<%= album.id %>'">
        <div class="card-artwork">
          <% if (album.artwork) { %>
          <img src="<%= album.artwork %>" alt="" loading="lazy">
          <% } else { %>
          <div class="card-artwork-placeholder">
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="12" cy="12" r="10"/><circle cx="12" cy="12" r="3"/></svg>
          </div>
          <% } %>
        </div>
        <div class="card-info">
          <div class="card-title"><%= album.title %></div>
          <div class="card-subtitle"><%= album.artist %> • <%= album.song_count %> tracks</div>
        </div>
      </div>
      <% }) %>
    </div>
  </section>
  <% } %>

  <% if (playlists.length > 0) { %>
  <section class="section">
    <div class="section-header">
      <h2>Playlists</h2>
      <a href="/playlists" class="see-all">See All</a>
    </div>
    <div class="card-row">
      <% playlists.forEach(function(pl) { %>
      <div class="card" onclick="window.location.href='/playlists/<%= pl.id %>'">
        <div class="card-artwork">
          <div class="card-artwork-placeholder" style="background: linear-gradient(135deg, #1a1a2e, #16213e);">
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M9 18V5l12-2v13"/><circle cx="6" cy="18" r="3"/><circle cx="18" cy="16" r="3"/></svg>
          </div>
        </div>
        <div class="card-info">
          <div class="card-title"><%= pl.name %></div>
          <div class="card-subtitle"><%= pl.song_count %> tracks</div>
        </div>
      </div>
      <% }) %>
    </div>
  </section>
  <% } %>
</div>
<%- include('partials/footer') %>
